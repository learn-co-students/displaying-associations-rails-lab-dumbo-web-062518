<!-- <h1><%= @artist.name %></h1>
<% @songs.each do |song| %>
  <ul>
    <li><%= link_to song.title, song_path(song) %></li>
  </ul>
<% end %> -->

<h1><%= @artist.name %></h1>
<h3>Songs:</h3>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>
